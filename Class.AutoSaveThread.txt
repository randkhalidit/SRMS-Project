/* Data Expert Task: Multithreading
   Demonstrates concurrent processes through an Auto-Save feature.
   Ensures system stability by saving records every 30 seconds. */
[span_13](start_span)public class AutoSaveThread extends Thread {[span_13](end_span)
    private SRMS srms; // Linked directly to the main system for data consistency
    [span_14](start_span)private boolean running = true;[span_14](end_span)

    public AutoSaveThread(SRMS srms) {
        this.srms = srms;
    }

    // Method to safely stop the thread
    public void stopThread() {
        [span_15](start_span)running = false;[span_15](end_span)
        [span_16](start_span)this.interrupt();[span_16](end_span)
    }

    @Override
    public void run() {
        [span_17](start_span)while (running) {[span_17](end_span)
            try {
                // Sleep for 30 seconds
                [span_18](start_span)Thread.sleep(30000);[span_18](end_span)
                
                // Fetch the live list from SRMS and save it
                [span_19](start_span)StudentFileManager.saveStudents(srms.getStudents());[span_19](end_span)
                [span_20](start_span)System.out.println("\n[System] Auto-save completed successfully.");[span_20](end_span)
                
            } catch (InterruptedException e) {
                [span_21](start_span)if (!running) break;[span_21](end_span)
                [span_22](start_span)System.out.println("Auto-save interrupted: " + e.getMessage());[span_22](end_span)
            }
        }
    }
}
